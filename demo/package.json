{
  "name": "demo",
  "version": "0.0.1",
  "license": "MIT",
  "private": true,
  "scripts": {
    "tsc": "tsc",
    "preview": "yarn build && serve public",
    "build": "./build.sh",
    "deploy": "yarn build && vercel"
  },
  "dependencies": {
    "@prisma/client": "2.28.0"
  },
  "devDependencies": {
    "@vercel/node": "1.11.1",
    "prisma": "2.28.0",
    "serve": "12.0.0",
    "vercel": "23.0.1"
  }
}
