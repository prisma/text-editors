import { test } from "@playwright/test";

test.describe("Prisma Query extension", () => {
  test("dims all lines that aren't part of a query", async ({ page }) => {
    test.fixme();
  });
  test("changes dimmed lines when the PrismaClinet variable name is changed", async ({
    page,
  }) => {
    test.fixme();
  });
  test("shows grey bars next to inactive queries", async ({ page }) => {
    test.fixme();
  });
  test("shows green bars next to active queries", async ({ page }) => {
    test.fixme();
  });
  test("shows grey run buttons next to inactive queries", async ({ page }) => {
    test.fixme();
  });
  test("shows green run buttons next to active queries", async ({ page }) => {
    test.fixme();
  });
});
